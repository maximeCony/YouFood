<?xml version="1.0" encoding="UTF-8"?>
<plats>
    {% for plat in plats %}
    <plat id="{{ plat.id }}">
        <name>{{ plat.name }}</name>
        <description>{{ plat.description }}</description>
    </plat>
    {% endfor %} 
</plats>